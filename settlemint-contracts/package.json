{
  "name": "settlemint-contracts",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "hardhat compile",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "test": "hardhat test",
    "deploy:localhost": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:amoy": "hardhat run scripts/deploy.ts --network amoy",
    "deploy:polygon": "hardhat run scripts/deploy.ts --network polygon",
    "bootstrap:localhost": "hardhat run scripts/bootstrapLocalhost.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.15.3",
    "hardhat": "^2.22.18",
    "typescript": "^5.8.2"
  }
}
