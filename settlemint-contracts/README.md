# SettleMint Contracts

`settlemint-contracts` contains the SettleMint settlement contract and the tooling used to deploy it. This package supports the blockchain-backed payment proof layer used by the application.

For the full technical overview of how this package fits into the system, see [../docs/docs.md](../docs/docs.md).
