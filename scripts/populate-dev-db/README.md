# populate-dev-db

`populate-dev-db` seeds a local development database with baseline SettleMint data so the application can be exercised against a known starting state.

For the broader project context, see [../../docs/docs.md](../../docs/docs.md).
