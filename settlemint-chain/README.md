# SettleMint Chain

`settlemint-chain` contains the shared blockchain network definitions used across the repository. It keeps the frontend and contract tooling aligned on chain configuration and payment-rail assumptions.

For the full technical overview of how this package fits into the system, see [../docs/docs.md](../docs/docs.md).
