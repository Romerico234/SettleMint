# SettleMint Service

`settlemint-service` is the backend API for SettleMint. It owns application state, business rules, persistence, settlement planning, payment verification, and archive handling.

For the full technical overview of how this package fits into the system, see [../docs/docs.md](../docs/docs.md).
