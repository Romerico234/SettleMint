services:
  settlement-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: settlement-service
    ports:
      - "8080:8080"
    env_file:
      - ./.env
    restart: unless-stopped
